services:
  dashboard:
    build: .
    container_name: arcadia-bi-dashboard
    ports:
      - "8501:8501"
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
